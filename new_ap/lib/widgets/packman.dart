import 'package:flutter/material.dart';

class Packman extends StatelessWidget {
  const Packman({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 15.0,
    );
  }
}
